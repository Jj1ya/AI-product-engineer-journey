services:
  api:
    build: .
    image: ai-todo-api:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/todos.db
    volumes:
      - ./data:/app/data